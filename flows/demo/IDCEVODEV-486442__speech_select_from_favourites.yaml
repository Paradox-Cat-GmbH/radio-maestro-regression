appId: com.bmwgroup.idnext.launcher
name: IDCEVODEV-486442 - Speech - select station from Favourites
tags: [demo, radio, speech, manual_step]
---
- evalScript: "${output.testId = 'IDCEVODEV-486442'}"
- runFlow: ../subflows/open_media.yaml
- runFlow: ../subflows/open_favourites.yaml

# Manual step:
# Trigger voice assistant and select/tune a station from the Favourites list.
# If your build exposes a tappable mic UI element, capture its id in Maestro Studio and add a tap step.
- waitForAnimationToEnd
- runFlow: ../subflows/verify_radio_backend.yaml
